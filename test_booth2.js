async function run() { const res = await fetch('https://booth.pm/en/items/3681787'); const html = await res.text(); const match = html.match(/wish_list_count&quot;:([0-9]+)/); console.log(match ? 'Found in JSON: ' + match[1] : 'Not found in JSON'); const match2 = html.match(/wish_lists_count&quot;:([0-9]+)/); console.log(match2 ? 'Found in JSON2: ' + match2[1] : 'Not found in JSON2'); const match3 = html.match(/wish[A-Za-z_]*count.*?([0-9]{3,7})/i); console.log(match3 ? 'Found via general regex: ' + match3[0] : 'Not found'); } run();
